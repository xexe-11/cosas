pipeline {
  agent {
    docker { image 'node:16-alpine3.12' }
  }

  stages {
    stage('Install') {
      steps { sh 'npm install' }
    }

    stage('Build') {
      steps { sh 'npm run-script build' }
    }
  }
}
